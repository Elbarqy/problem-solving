#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef pair<int, int> ii;
typedef vector<int> vi;
typedef vector<ii> vii;

void inputRoutine()
{
    int Tc, a, b;
    scanf("%d", &Tc);
    while (Tc--)
    {
        scanf("%d %d", &a, &b);
    }
    while (*(scanf("%d %d"), &a, &b) != EOF)
    {
        printf("processing the %d and %d", &a, &b);
    }
}