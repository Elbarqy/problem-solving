#include <bits/stdc++.h>

using namespace std;

struct node{
    int value;
    struct node * left;
    struct node * right;
};

int diameter(node * myNode){

}
int main(){
    node * x = (node * )malloc(sizeof(node));;
    cout<<diameter(x)<<endl;
    return 0;
}