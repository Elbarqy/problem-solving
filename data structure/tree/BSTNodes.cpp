#include <bits/stdc++.h>

using namespace std;

struct node{
    int n;
    struct node * left;
    struct node * right;
};

int main(){
    struct node * temp = (struct node *) malloc(sizeof(struct node));
    
    return 0;
}